twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['form/tailwind_layout.html.twig']
    globals:
        oav_context_enabled: false

when@test:
    twig:
        strict_variables: true
