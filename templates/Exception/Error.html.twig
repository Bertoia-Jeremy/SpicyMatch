{% extends 'base.html.twig' %}

{% block title %}SpicyMatch — Erreur{% endblock %}

{% block body %}
{% import 'macros/buttons.html.twig' as buttons %}

<section class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-24 text-center">
    <div class="bg-white border border-stone-200 rounded-2xl p-10">
        <div class="w-16 h-16 rounded-full bg-red-50 flex items-center justify-center mx-auto mb-6">
            <i class="fa-solid fa-triangle-exclamation text-red-500 text-2xl" aria-hidden="true"></i>
        </div>
        <h1 class="text-3xl font-bold text-stone-900 mb-3">{{ 'ui.error.title'|trans }}</h1>
        {% if codeError is defined %}
            <p class="text-stone-400 text-sm mb-2">{{ 'ui.error.code'|trans }} <code class="bg-stone-100 px-2 py-0.5 rounded text-stone-600">{{ codeError }}</code></p>
        {% endif %}
        <p class="text-stone-500 mb-8">{{ 'ui.error.persist'|trans }}</p>
        <div class="flex gap-3 justify-center">
            {{ buttons.button('ui.error.home'|trans, {
                href: path('home'),
                variant: 'primary',
                icon: 'fa-solid fa-house'
            }) }}
            {{ buttons.button('Signaler', {
                href: path('new_contact'),
                variant: 'outline',
                icon: 'fa-solid fa-envelope'
            }) }}
        </div>
    </div>
</section>
{% endblock %}
