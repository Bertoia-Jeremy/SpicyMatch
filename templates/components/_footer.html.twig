<footer class="bg-paprika-900 mt-auto">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-16 pb-10">

        {# ── 4 colonnes ── #}
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-10 mb-14">

            {# Col 1 : Logo + description #}
            <div class="sm:col-span-2 lg:col-span-1">
                <a href="{{ path('home') }}"
                   class="inline-flex items-center gap-2 font-display text-xl font-bold text-cream tracking-tight mb-5">
                    <i class="fa-solid fa-fire text-turmeric-400"></i>
                    Spicy<span class="italic text-turmeric-400">Match</span>
                </a>
                <p class="text-cream/60 text-sm leading-relaxed max-w-xs">
                    Le moteur d'association aromatique basé sur la science des composés. Composez avec précision.
                </p>
                <div class="flex items-center gap-3 mt-6">
                    <span class="inline-flex items-center justify-center w-8 h-8 rounded-full bg-cream/5 border border-cream/10 text-cream/40 hover:text-cream hover:border-cream/30 transition-colors cursor-pointer text-xs">
                        <i class="fa-brands fa-instagram"></i>
                    </span>
                    <span class="inline-flex items-center justify-center w-8 h-8 rounded-full bg-cream/5 border border-cream/10 text-cream/40 hover:text-cream hover:border-cream/30 transition-colors cursor-pointer text-xs">
                        <i class="fa-brands fa-x-twitter"></i>
                    </span>
                </div>
            </div>

            {# Col 2 : Navigation #}
            <div>
                <h5 class="text-cream font-semibold text-xs uppercase tracking-widest mb-5">Navigation</h5>
                <ul class="space-y-3">
                    <li>
                        <a href="{{ path('home') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Accueil</a>
                    </li>
                    <li>
                        <a href="{{ path('index_spicy_match') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Créer un mélange</a>
                    </li>
                    <li>
                        <a href="{{ path('index_spices') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Toutes les épices</a>
                    </li>
                    <li>
                        <a href="{{ path('new_contact') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Contact</a>
                    </li>
                </ul>
            </div>

            {# Col 3 : Explorer #}
            <div>
                <h5 class="text-cream font-semibold text-xs uppercase tracking-widest mb-5">Explorer</h5>
                <ul class="space-y-3">
                    <li>
                        <a href="{{ path('index_aromatic_compound') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Composés aromatiques</a>
                    </li>
                    <li>
                        <a href="{{ path('index_aromatic_groups') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Groupes aromatiques</a>
                    </li>
                    <li>
                        <a href="{{ path('index_spicy_type') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Types d'épices</a>
                    </li>
                    <li>
                        <a href="{{ path('index_alchemy_flavors') }}"
                           class="text-cream/60 hover:text-cream text-sm transition-colors">Saveurs</a>
                    </li>
                </ul>
            </div>

            {# Col 4 : Newsletter #}
            <div>
                <h5 class="text-cream font-semibold text-xs uppercase tracking-widest mb-5">Newsletter</h5>
                <p class="text-cream/60 text-sm mb-5 leading-relaxed">
                    Recevez les nouveautés & recettes directement dans votre boîte mail.
                </p>
                <div class="space-y-2.5">
                    <label for="newsletter1" class="sr-only">Adresse email</label>
                    <input id="newsletter1" type="email"
                           class="w-full bg-white/5 border border-cream/10 text-cream placeholder-cream/30
                                  rounded-lg px-4 py-2.5 text-sm
                                  focus:outline-none focus:ring-1 focus:ring-turmeric-400 focus:border-turmeric-400/50
                                  transition-colors"
                           placeholder="votre@email.com">
                    <button class="w-full bg-turmeric-500 hover:bg-turmeric-400 text-stone-900 font-semibold
                                   rounded-lg px-4 py-2.5 text-sm transition-colors"
                            type="button">
                        S'abonner
                    </button>
                </div>
            </div>
        </div>

        {# ── Bas de page ── #}
        <div class="border-t border-cream/10 pt-8 flex flex-col sm:flex-row items-center justify-between gap-4">
            <p class="text-cream/40 text-sm">© {{ "now"|date("Y") }} SpicyMatch. Tous droits réservés.</p>
            <div class="flex items-center gap-1.5 text-cream/30 text-xs">
                <i class="fa-solid fa-fire text-turmeric-400 text-xs"></i>
                <span>Fait avec passion pour les épices</span>
            </div>
        </div>
    </div>
</footer>
