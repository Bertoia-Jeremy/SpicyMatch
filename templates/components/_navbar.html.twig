<nav x-data="{ open: false }" class="bg-white shadow-sm">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center py-4">
            <div>
                <a class="text-xl font-bold" href="{{ path('home') }}">SpicyMatch</a>
            </div>

            <div class="hidden md:flex items-center space-x-4">
                <a class="text-gray-600 hover:text-gray-800" href="{{ path('home') }}">Accueil</a>
                <a class="text-gray-600 hover:text-gray-800" href="{{ path('index_spicy_match') }}">Créer mon mélange</a>

                <div x-data="{ dropdownOpen: false }" class="relative">
                    <button @click="dropdownOpen = !dropdownOpen" class="text-gray-600 hover:text-gray-800 flex items-center">
                        Epices
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                    </button>
                    <div x-show="dropdownOpen" @click.away="dropdownOpen = false" class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg z-20">
                        <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_aromatic_compound') }}">Par composé aromatique</a>
                        <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_aromatic_groups') }}">Par groupe aromatique</a>
                        <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_spicy_type') }}">Par type d'épice</a>
                        <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_alchemy_flavors') }}">Par saveur</a>
                        <div class="border-t border-gray-100"></div>
                        <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_spices') }}">Toutes les épices</a>
                    </div>
                </div>

                <a class="text-gray-600 hover:text-gray-800" href="{{ path('new_contact') }}">Contact</a>
            </div>

            <div class="hidden md:flex items-center space-x-4">
                <form class="flex" role="search">
                    {{ component('Search') }}
                </form>

                {% if is_granted("ROLE_USER") %}
                    <div x-data="{ dropdownOpen: false }" class="relative">
                        <button @click="dropdownOpen = !dropdownOpen" class="flex items-center text-gray-600 hover:text-gray-800">
                            {{ app.user.username|capitalize }}
                            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                        </button>
                        <div x-show="dropdownOpen" @click.away="dropdownOpen = false" class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg z-20">
                            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('profile_user') }}">Profil</a>
                            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('configuration_user') }}">Configuration</a>
                            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('history_user') }}">Historique</a>
                            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('security_user') }}">Sécurité</a>
                            <div class="border-t border-gray-100"></div>
                            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('app_logout') }}">Déconnexion</a>
                        </div>
                    </div>
                {% else %}
                    <a class="text-gray-600 hover:text-gray-800" href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </div>

            <div class="md:hidden flex items-center">
                <button @click="open = !open" class="text-gray-600 hover:text-gray-800 focus:outline-none">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                </button>
            </div>
        </div>

        <div x-show="open" class="md:hidden">
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('home') }}">Accueil</a>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_spicy_match') }}">Créer mon mélange</a>
            <div class="border-t border-gray-200"></div>
            <h3 class="px-4 py-2 text-xs text-gray-500 font-semibold uppercase">Epices</h3>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_aromatic_compound') }}">Par composé aromatique</a>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_aromatic_groups') }}">Par groupe aromatique</a>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_spicy_type') }}">Par type d'épice</a>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_alchemy_flavors') }}">Par saveur</a>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('index_spices') }}">Toutes les épices</a>
            <div class="border-t border-gray-200"></div>
            <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('new_contact') }}">Contact</a>
            <div class="border-t border-gray-200"></div>
            {% if is_granted("ROLE_USER") %}
                <h3 class="px-4 py-2 text-xs text-gray-500 font-semibold uppercase">Mon Compte</h3>
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('profile_user') }}">Profil</a>
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('configuration_user') }}">Configuration</a>
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('history_user') }}">Historique</a>
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('security_user') }}">Sécurité</a>
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>