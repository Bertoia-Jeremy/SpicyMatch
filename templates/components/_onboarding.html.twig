{#
    Onboarding guidé — 2 couches :
    1. Modale de bienvenue (accueil uniquement)
    2. Spotlight tour contextuel (pages cibles : épices, lab, académie)

    Logique JS : assets/alpine_components.js (onboardingWelcome, spotlightTour).
    Configuration des tours (targets, titres, textes) passée via data-tours-json
    car elle contient des path() Twig résolus côté serveur.
#}
{% if is_granted('ROLE_USER') %}

{% set tours = {
    spices: {
        path: '/epices',
        nextState: 'lab',
        nextUrl: path('index_spicy_match'),
        transitionTitle: 'ui.onboarding.transition_spices'|trans,
        steps: [
            {
                target: 'input[name="search"]',
                title: 'ui.onboarding.s1_search_title'|trans,
                text: 'ui.onboarding.s1_search_text'|trans,
                position: 'bottom',
                noClickAdvance: true
            },
            {
                target: '.card-warm',
                title: 'ui.onboarding.s1_cards_title'|trans,
                text: 'ui.onboarding.s1_cards_text'|trans,
                position: 'bottom',
                followModal: {
                    title: 'ui.onboarding.s1_modal_title'|trans,
                    text: 'ui.onboarding.s1_modal_text'|trans,
                    position: 'top'
                }
            },
            {
                target: '[data-tour="cta-lab"]',
                title: 'ui.onboarding.s1_cta_title'|trans,
                text: 'ui.onboarding.s1_cta_text'|trans,
                position: 'bottom'
            }
        ]
    },
    lab: {
        path: '/spicymatch',
        nextState: 'academy',
        nextUrl: path('education_index'),
        transitionTitle: 'ui.onboarding.transition_lab'|trans,
        steps: [
            {
                target: '[data-tour="mode-toggle"]',
                title: 'ui.onboarding.s2_mode_title'|trans,
                text: 'ui.onboarding.s2_mode_text'|trans,
                position: 'bottom',
                noClickAdvance: true
            },
            {
                target: '[data-tour="lab-aromatic-filter"]',
                title: 'ui.onboarding.s2_filter_title'|trans,
                text: 'ui.onboarding.s2_filter_text'|trans,
                position: 'bottom',
                noClickAdvance: true
            },
            {
                target: '.card-warm',
                title: 'ui.onboarding.s2_play_title'|trans,
                text: 'ui.onboarding.s2_play_text'|trans,
                position: 'top',
                noClickAdvance: true
            }
        ]
    },
    academy: {
        path: '/education',
        nextState: 'done',
        nextUrl: null,
        transitionTitle: null,
        steps: [
            {
                target: '[data-tour="game-grid"]',
                title: 'ui.onboarding.s3_games_title'|trans,
                text: 'ui.onboarding.s3_games_text'|trans,
                position: 'bottom'
            },
            {
                target: '.tag-secondary',
                title: 'ui.onboarding.s3_xp_title'|trans,
                text: 'ui.onboarding.s3_xp_text'|trans,
                position: 'left'
            }
        ]
    }
} %}

{# ═══════════════════════════════════════════════════════════ #}
{# COUCHE 1 — Modale de bienvenue (accueil uniquement)        #}
{# ═══════════════════════════════════════════════════════════ #}
<div x-data="onboardingWelcome"
     x-show="visible"
     x-cloak
     x-transition:enter="transition ease-out duration-300"
     x-transition:enter-start="opacity-0 scale-95"
     x-transition:enter-end="opacity-100 scale-100"
     @keydown.escape="skip"
     @keydown.tab="handleTab($event)"
     class="fixed inset-0 z-60 flex items-end sm:items-center justify-center p-4 sm:p-8"
     role="dialog" aria-modal="true" aria-labelledby="onboarding-welcome-title">

    <div class="absolute inset-0 bg-stone-900/60 backdrop-blur-sm"
         @click="skip"></div>

    <div class="relative z-10 w-full max-w-sm bg-cream rounded-2xl shadow-2xl border border-saffron-200/60 overflow-hidden">

        <button @click="skip"
                class="absolute top-4 right-4 w-7 h-7 flex items-center justify-center rounded-full
                       text-stone-400 hover:text-stone-600 hover:bg-stone-100 transition-colors z-10"
                aria-label="{{ 'ui.onboarding.skip_intro'|trans }}">
            <i class="fa-solid fa-xmark text-xs"></i>
        </button>

        <div class="px-7 pt-8 pb-7">
            <div class="text-center mb-6">
                <div class="w-20 h-20 bg-saffron-100 rounded-2xl flex items-center justify-center mx-auto mb-5 border border-saffron-200">
                    <i class="fa-solid fa-hand-sparkles text-4xl text-saffron-600"></i>
                </div>
                <h2 id="onboarding-welcome-title" class="text-xl font-bold text-stone-800 mb-3 leading-tight">
                    {{ 'ui.onboarding.welcome_title'|trans }}
                </h2>
                <p class="text-stone-500 text-sm leading-relaxed">
                    {{ 'ui.onboarding.welcome_text_html'|trans|raw }}
                </p>
            </div>
            <a href="{{ path('index_spices') }}"
               @click="start"
               class="flex items-center justify-center gap-2 w-full px-5 py-3.5 bg-paprika-800
                      text-white text-sm font-bold rounded-xl hover:bg-paprika-900
                      transition-colors shadow-sm shadow-paprika-900/20 mb-3">
                <i class="fa-solid fa-arrow-right"></i>
                {{ 'ui.onboarding.lets_go'|trans }}
            </a>
            <button @click="skip"
                    class="w-full text-center text-xs text-stone-500 hover:text-stone-600 py-1 transition-colors">
                {{ 'ui.onboarding.already_know'|trans }}
            </button>
        </div>
    </div>
</div>

{# ═══════════════════════════════════════════════════════════ #}
{# COUCHE 2 — Spotlight tour (pages cibles)                    #}
{# ═══════════════════════════════════════════════════════════ #}
<div x-data="spotlightTour"
     data-tours-json="{{ tours|json_encode|e('html_attr') }}"
     x-cloak>

    <div x-show="active"
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="opacity-0"
         x-transition:enter-end="opacity-100"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0"
         class="spotlight-overlay"
         :style="spotlightStyle"
         @click="next">
    </div>

    <div x-show="tooltipActive"
         x-transition:enter="transition ease-out duration-200 delay-100"
         x-transition:enter-start="opacity-0 translate-y-2"
         x-transition:enter-end="opacity-100 translate-y-0"
         x-transition:leave="transition ease-in duration-150"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0"
         class="fixed z-60 max-w-xs bg-white rounded-xl shadow-2xl border border-saffron-200/60 p-5"
         :style="tooltipStyle"
         role="tooltip"
         @keydown.escape.window="skip">

        <p class="text-sm font-semibold text-stone-800 mb-1" x-text="currentTitle"></p>
        <p class="text-xs text-stone-600 leading-relaxed mb-4" x-text="currentText"></p>

        <div class="flex items-center justify-between">
            <div class="flex gap-1.5">
                <template x-for="i in totalSteps" :key="i">
                    <div :class="stepDotClass(i)"
                         class="transition-all duration-300"></div>
                </template>
            </div>
            <div class="flex gap-2">
                <button @click="skip" class="text-xs text-stone-500 hover:text-stone-600 transition-colors">{{ 'ui.onboarding.skip'|trans }}</button>
                <button @click="next"
                        aria-label="{{ 'ui.onboarding.next_step_aria'|trans }}"
                        class="text-xs font-semibold text-white bg-paprika-800 hover:bg-paprika-900 px-3 py-1.5 rounded-full transition-colors">
                    <span x-text="nextLabel()"></span>
                </button>
            </div>
        </div>
    </div>

    <div x-show="showTransition"
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="opacity-0 translate-y-4"
         x-transition:enter-end="opacity-100 translate-y-0"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0 translate-y-4"
         class="fixed bottom-8 left-1/2 -translate-x-1/2 z-60 bg-white rounded-xl shadow-2xl border border-saffron-200/60 px-6 py-4 max-w-sm">
        <p class="text-sm font-semibold text-stone-800 mb-2" x-text="transitionTitle"></p>
        <div class="flex gap-2 justify-end">
            <button @click="skipTransition" class="text-xs text-stone-500 hover:text-stone-600">{{ 'ui.onboarding.later'|trans }}</button>
            <a :href="transitionUrl"
               @click="acceptTransition"
               class="text-xs font-semibold text-white bg-paprika-800 hover:bg-paprika-900 px-3 py-1.5 rounded-full transition-colors">
                {{ 'ui.onboarding.go'|trans }}
            </a>
        </div>
    </div>
</div>

{% endif %}
