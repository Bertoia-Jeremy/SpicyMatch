{# ═══════════════════════════════════════════════════════
   Chapitre IV — Le Théâtre des Sens
   3 affiches apothicaire + CTA Académie
   ═══════════════════════════════════════════════════════ #}

<section id="theatre" class="home-section"
         style="background: linear-gradient(180deg, var(--color-cream-dark) 0%, var(--color-cream) 100%)">

    {# Section header #}
    <div class="section-head">
        <div>
            <div class="section-label">{{ 'ui.theatre.chapter'|trans }}</div>
            <h2 class="font-display font-medium leading-none tracking-[-0.015em] m-0"
                style="font-size: clamp(40px, 5vw, 80px)">
                {{ 'ui.theatre.title_html'|trans|raw }}
            </h2>
        </div>
        <p class="section-blurb">
            {{ 'ui.theatre.blurb'|trans }}
        </p>
    </div>

    {# Grille affiches #}
    <div class="poster-grid">

        {# ── Affiche I : Défi de Scoville (Survie) ── #}
        <a href="{{ path('education_index') }}" class="poster p1"
           style="background: radial-gradient(ellipse at 50% 0%, #C04020 0%, #7a1a1a 60%, #3a0a0a 100%)"
           aria-label="{{ 'ui.theatre.play_scoville'|trans }}">
            <div class="poster-top">
                <div class="poster-num">{{ 'ui.theatre.act1'|trans }}</div>
                <div class="poster-ornament" aria-hidden="true">✦</div>
            </div>
            <div class="poster-mid">
                <h3>{{ 'ui.theatre.scoville_title_html'|trans|raw }}</h3>
                <div class="poster-dash" aria-hidden="true"></div>
                <div class="poster-tag">{{ 'ui.theatre.scoville_tag'|trans }}</div>
            </div>
            <div class="poster-bot">
                <span>SURVIVAL</span>
                <span class="right">{{ 'ui.theatre.scoville_footer'|trans }}</span>
            </div>
        </a>

        {# ── Affiche II : Le Choix du Chef (L'Intrus) ── #}
        <a href="{{ path('education_index') }}" class="poster p2"
           style="background: radial-gradient(ellipse at 50% 0%, #4D7C0F 0%, #2d4a08 60%, #1a2a04 100%)"
           aria-label="{{ 'ui.theatre.play_choice'|trans }}">
            <div class="poster-top">
                <div class="poster-num">{{ 'ui.theatre.act2'|trans }}</div>
                <div class="poster-ornament" aria-hidden="true">✦</div>
            </div>
            <div class="poster-mid">
                <h3>{{ 'ui.theatre.choice_title_html'|trans|raw }}</h3>
                <div class="poster-dash" aria-hidden="true"></div>
                <div class="poster-tag">{{ 'ui.theatre.choice_tag'|trans }}</div>
            </div>
            <div class="poster-bot">
                <span>INTRUS</span>
                <span class="right">{{ 'ui.theatre.choice_footer'|trans }}</span>
            </div>
        </a>

        {# ── Affiche III : Trop Cuit! (Pendu) ── #}
        <a href="{{ path('education_index') }}" class="poster p3"
           style="background: radial-gradient(ellipse at 50% 0%, #D97706 0%, #7c3a04 60%, #3a1c02 100%)"
           aria-label="{{ 'ui.theatre.play_overcooked'|trans }}">
            <div class="poster-top">
                <div class="poster-num">{{ 'ui.theatre.act3'|trans }}</div>
                <div class="poster-ornament" aria-hidden="true">✦</div>
            </div>
            <div class="poster-mid">
                <h3>{{ 'ui.theatre.overcooked_title_html'|trans|raw }}</h3>
                <div class="poster-dash" aria-hidden="true"></div>
                <div class="poster-tag">{{ 'ui.theatre.overcooked_tag'|trans }}</div>
            </div>
            <div class="poster-bot">
                <span>HANGMAN</span>
                <span class="right">{{ 'ui.theatre.overcooked_footer'|trans }}</span>
            </div>
        </a>

    </div>

    {# CTA Académie #}
    <div class="theatre-cta">
        <p class="theatre-cta-sub">
            {{ 'ui.theatre.cta_sub'|trans }}
        </p>
        <a href="{{ path('education_index') }}" class="btn-pill-primary text-base px-8 py-4">
            {{ 'ui.theatre.cta'|trans }}
            <span aria-hidden="true" class="ml-2">→</span>
        </a>
    </div>

</section>
