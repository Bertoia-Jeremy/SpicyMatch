{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<div class="flex min-h-[calc(100vh-4rem)]">
    <div class="w-1/2 hidden md:block">
        <img src="{{ asset('assets/images/login.png') }}" alt="Épices" class="w-full h-full object-cover">
    </div>
    <div class="w-full md:w-1/2 flex justify-center items-center bg-[#F9F5F0]">
        <div class="w-full max-w-md p-8">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-14 h-14 bg-orange-100 rounded-full mb-4">
                    <i class="fa-solid fa-fire text-2xl text-orange-600"></i>
                </div>
                <h2 class="text-3xl font-bold text-stone-800">Connexion</h2>
                <p class="text-stone-500 mt-2 text-sm">
                    <a href="{{ path('index_register') }}" class="text-orange-600 hover:text-orange-700 hover:underline">Pas de compte ? En créer un (même pas besoin de mail !)</a>
                </p>
            </div>

            <form action="{{ path('app_login') }}" method="post" class="space-y-5">
                <div>
                    <label for="username" class="block text-sm font-medium text-stone-700 mb-1">Pseudo</label>
                    <input type="text" id="username" name="username" value="{{ last_username }}"
                           class="w-full px-4 py-2.5 border border-stone-300 rounded-lg focus:outline-none focus:ring-1 focus:ring-orange-500 focus:border-orange-500 bg-white text-stone-800"
                           required autofocus>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-stone-700 mb-1">Mot de passe</label>
                    <div class="relative" x-data="{ show: false }">
                        <input :type="show ? 'text' : 'password'" id="password" name="password"
                               class="w-full px-4 py-2.5 pr-24 border border-stone-300 rounded-lg focus:outline-none focus:ring-1 focus:ring-orange-500 focus:border-orange-500 bg-white text-stone-800"
                               required>
                        <button type="button" @click="show = !show"
                                class="absolute inset-y-0 right-0 px-3 flex items-center text-sm text-stone-500 hover:text-stone-700">
                            <span x-text="show ? 'Masquer' : 'Afficher'">Afficher</span>
                        </button>
                    </div>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                <input type="hidden" name="_target_path" value="{{ path('home') }}">

                {% if error is not empty %}
                    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg text-sm" role="alert">
                        <i class="fa-solid fa-triangle-exclamation mr-1.5"></i>Identifiants incorrects.
                    </div>
                {% endif %}

                <button type="submit"
                        class="w-full px-4 py-3 text-white bg-orange-600 hover:bg-orange-700 rounded-lg font-semibold transition-colors mt-2">
                    Se connecter
                </button>
            </form>
        </div>
    </div>
</div>
{% endblock %}
