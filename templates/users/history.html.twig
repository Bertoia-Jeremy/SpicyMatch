{% extends 'users/dashboard.html.twig' %}

{% block content %}
    <turbo-frame id="user_frame_id">
        {% include 'components/_user_tabs.html.twig' %}

        <div class="mt-6">
            <h2 class="text-xl font-semibold text-stone-800 mb-4 flex items-center gap-2">
                <i class="fa-solid fa-clock-rotate-left text-saffron-600 text-base"></i>Historique des mélanges
            </h2>
            <div class="card-warm p-5 text-center">
                <p class="text-stone-500 text-sm mb-4">Retrouvez toutes vos recettes sauvegardées.</p>
                <a href="{{ path('index_spicy_match_history') }}" class="btn-pill-primary">
                    <i class="fa-solid fa-clock-rotate-left"></i>Voir l'historique complet
                </a>
            </div>
        </div>
    </turbo-frame>
{% endblock %}
